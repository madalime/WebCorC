type: object
description: >-
  A condition in the editor's condition language. Must not be empty; the
  backend returns it re-printed in its own normalized form.
properties:
  condition:
    type: string

required:
  - condition
