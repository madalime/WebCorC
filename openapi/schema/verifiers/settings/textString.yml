description: VerifierTextStringSetting
allOf:
  - $ref: './stringValued.yml'
  - type: object
    properties:
      type:
        type: string
        enum:
          - text

      valueType:
        type: string
        enum:
          - string
        description: May be omitted; a free-text setting defaults to `string`.

    required:
      - type