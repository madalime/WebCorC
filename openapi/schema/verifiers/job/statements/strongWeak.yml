description: >-
  A strong/weak refinement wrapper around one nested statement.
allOf:
  - $ref: './abstract.yml'
  - type: object
    properties:
      statement:
        $ref: '../statement.yml'
    required:
      - statement
